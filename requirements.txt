# ==============================================================================
# CORE ML & COMPUTER VISION
# Dependências essenciais para o modelo e processamento de imagem.
# ==============================================================================
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.3.0
numpy>=1.24.0
opencv-python-headless>=4.8.0  # Versão sem dependências de GUI, ideal para servidores
pillow>=10.0.0

# ==============================================================================
# MULTI-OBJECT TRACKING
# Bibliotecas para o rastreamento de veículos, se usado em produção.
# ==============================================================================
filterpy>=1.4.5
lap>=0.4.0

# ==============================================================================
# API & WEB SERVER
# Framework da API e servidor web para produção.
# ==============================================================================
fastapi>=0.104.0
gunicorn>=21.2.0              # Servidor WSGI/ASGI para produção
uvicorn[standard]>=0.24.0      # Servidor ASGI que o Gunicorn vai gerenciar
python-multipart>=0.0.6       # Necessário para upload de arquivos em formulários
orjson>=3.9.0                 # Parser JSON mais rápido para o FastAPI

# ==============================================================================
# UTILITIES & LOGGING
# Utilitários para configuração, logging e processamento.
# ==============================================================================
pyyaml>=6.0
python-dotenv>=1.0.0
loguru>=0.7.0                 # Logging simples e poderoso
imageio-ffmpeg>=0.4.9         # Essencial para processar vídeos